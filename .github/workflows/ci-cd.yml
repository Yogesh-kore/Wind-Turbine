name: MLflow Wind Turbine Training CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-train:
    runs-on: windows-latest

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v3

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🧠 Run training script
        run: |
          python src/train.py

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: 🔨 Build Docker image
        run: docker build -t windturbine-train .

      # Optional: Push image to DockerHub (you must configure DockerHub secrets in GitHub repo)
      # - name: 🔑 Log in to DockerHub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: 🚀 Push Docker image
      #   run: docker tag windturbine-train yourdockerhubuser/windturbine-train:latest && docker push yourdockerhubuser/windturbine-train:latest
